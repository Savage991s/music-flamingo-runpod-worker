# Music Flamingo + RunPod handler dependencies.
# torch is provided by the runpod/pytorch base image and intentionally NOT pinned here
# to avoid version conflicts. We pin the audio/HF stack to versions known to work
# with Music Flamingo / Audio Flamingo 3.

runpod>=1.7
# AudioFlamingo3ForConditionalGeneration was added to transformers in May 2026
# and isn't in any tagged release yet — install from git main.
transformers @ git+https://github.com/huggingface/transformers.git@main
accelerate>=0.34
huggingface_hub>=0.26
librosa>=0.10.2
soundfile>=0.12.1
requests>=2.32
